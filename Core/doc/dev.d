/** \file Core/doc/dev.d

  \page DevPage Developer's Guide

  Hello! Clean is a big engine. Its main purpose is to provide an interface for game developers, on 
  all supportable platforms. It dynamically loads Core::Driver interfaces to provide different 3D API 
  to developpers, like OpenGL or DirectX (or Mesa, if you want to create one). 

  This guide is here to provide some explanations on how to create a program with the Clean engine. First
  of all, you have to create a directory for your application. Let's take '~/Clean/MyApp'. Using git, clone 
  the repository to your directory.
